{
  "buildCommand": "cd frontend/grocerylyzerfront && bun install && bun run build:vercel",
  "outputDirectory": "frontend/grocerylyzerfront/dist",
  "installCommand": "echo 'Skipping root install'",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
