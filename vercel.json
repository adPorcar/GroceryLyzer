{
  "buildCommand": "cd frontend/grocerylyzerfront && npm install --legacy-peer-deps && npm run build:vercel",
  "outputDirectory": "frontend/grocerylyzerfront/dist",
  "installCommand": "echo 'Skipping root install'",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
